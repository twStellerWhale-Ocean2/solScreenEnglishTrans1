<UserControl x:Class="ScreenTrans.Present.NotesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <!-- 左側欄寬可拖拉調整（Issue #58）：GridSplitter 於中間 Auto 欄 -->
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="212" MinWidth="150"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*" MinWidth="240"/>
    </Grid.ColumnDefinitions>

    <!-- 左：頂部工具列（僅建立資料夾）＋標準多層資料夾樹（右鍵選單／F2 原地更名，Issue #38） -->
    <Grid Grid.Column="0" Margin="10,8,4,10">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <Button x:Name="NewFolderBtn" Grid.Row="0" Content="＋ 建立資料夾" Style="{StaticResource GhostButton}"
              HorizontalAlignment="Left" Padding="10,4" Margin="0,0,0,6"/>
      <Border Grid.Row="1" Style="{StaticResource Card}">
        <TreeView x:Name="FolderTree" BorderThickness="0" Background="Transparent" AllowDrop="True" Margin="2"/>
      </Border>
    </Grid>

    <GridSplitter Grid.Column="1" Style="{StaticResource ColSplitter}"/>

    <!-- 右：選取夾之條目（右欄頂[清除全部]，比照歷史分頁；Issue #42） -->
    <Grid Grid.Column="2" Margin="4,8,10,10">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <!-- 條目工具列（Issue #52 排序鈕於[清除全部]左側；均右對齊比照歷史分頁） -->
      <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,6">
        <Button x:Name="SortAscBtn" Content="順向排序" Style="{StaticResource GhostButton}"
                ToolTip="依原文 A→Z 排序目前資料夾條目" Margin="0,0,6,0"/>
        <Button x:Name="SortDescBtn" Content="反向排序" Style="{StaticResource GhostButton}"
                ToolTip="依原文 Z→A 排序目前資料夾條目" Margin="0,0,6,0"/>
        <Button x:Name="ClearEntriesBtn" Content="清除全部"
                Foreground="#8A2B2B" Style="{StaticResource GhostButton}"/>
      </StackPanel>
      <Grid Grid.Row="1">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8,0,4,0">
          <StackPanel x:Name="EntryPanel"/>
        </ScrollViewer>
        <TextBlock x:Name="EmptyHint" Text="此資料夾尚無筆記。在結果或歷史按「加入我的筆記」即可收藏；左側資料夾可右鍵管理、拖曳移動。"
                   Foreground="#9A6A82" FontSize="13" TextWrapping="Wrap" MaxWidth="320"
                   TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
      </Grid>
    </Grid>
  </Grid>
</UserControl>
